# 自動ブログ投稿機能 - セットアップ＆運用ガイド

美容クリニックの集客を目的とした、AIによるブログ記事の自動生成・投稿システムのセットアップと運用方法について解説します。

## 1. 機能概要

このシステムは、以下の3つの主要機能で構成されています。

1.  **SEOキーワード自動選定**
    - 美容医療に関連する多様なキーワード（施術、地域、悩みなど）を定義したリストから、使用履歴と頻度ルールに基づき、最適なキーワードを自動で選定します。

2.  **AIによる記事自動生成**
    - OpenAI API（`gpt-4.1-mini`）を利用し、選定されたキーワードを基にSEOに最適化された高品質なブログ記事（2000～3000文字）をMarkdown形式で自動生成します。
    - 医療広告ガイドラインを遵守し、専門的かつ信頼性の高いコンテンツを提供します。

3.  **WordPress自動投稿**
    - 生成された記事をWordPress REST API経由で自動的に投稿します。
    - 記事のタイトル、本文、カテゴリー、タグ（キーワードから自動生成）が設定されます。

## 2. システム構成

本システムは、GitHub Actionsを利用して定期的にPythonスクリプトを実行することで、一連の処理を自動化しています。

| コンポーネント | 説明 |
| :--- | :--- |
| **GitHub Actions** | `auto-blog-post.yml`ワークフローが毎日午前10時（日本時間）に処理を自動実行します。 |
| **Pythonスクリプト** | `scripts/`ディレクトリに格納された3つのスクリプトが、キーワード選定、記事生成、WordPress投稿の各処理を担います。 |
| **キーワードデータ** | `seo_keywords.json`に、ブログ記事のテーマとなるSEOキーワードが階層的に定義されています。 |
| **生成データ** | `data/`ディレクトリに、生成された記事（JSON, Markdown）やキーワードの使用履歴が保存されます。 |

### ファイル構成

```
beauty-clinic/
├── .github/workflows/
│   └── auto-blog-post.yml  # 自動投稿ワークフロー
├── scripts/
│   ├── auto_blog.py          # メイン実行スクリプト
│   ├── generate_article.py   # 記事生成スクリプト
│   └── post_to_wordpress.py  # WordPress投稿スクリプト
├── data/
│   ├── articles/             # 生成された記事ファイル
│   ├── keyword_history.json  # キーワード使用履歴
│   └── post_history/         # 投稿履歴
├── seo_keywords.json         # SEOキーワード定義ファイル
└── AUTO_BLOG_GUIDE.md        # このガイド
```

## 3. セットアップ手順

本機能を動作させるには、以下のリポジトリシークレットを設定する必要があります。

1.  リポジトリの **Settings** > **Secrets and variables** > **Actions** に移動します。
2.  **New repository secret** をクリックし、以下の4つのシークレットを追加します。

| シークレット名 | 説明 |
| :--- | :--- |
| `OPENAI_API_KEY` | OpenAI APIキー。記事の自動生成に使用します。 |
| `WP_SITE_URL` | WordPressサイトのURL。（例: `https://example.com`） |
| `WP_USER` | WordPressのユーザー名。投稿権限を持つユーザーを指定してください。 |
| `WP_APP_PASSWORD` | WordPressの**アプリケーションパスワード**。通常のパスワードではなく、管理画面の「ユーザー」→「プロフィール」から新規生成してください。 |

> **注意**: アプリケーションパスワードは、REST API経由での認証に必要です。安全な認証のため、必ず新規に生成してください。

## 4. 運用方法

### 自動投稿

-   セットアップが完了すると、**毎日午前10時（日本時間）**にGitHub Actionsが自動的に実行され、新しいブログ記事が1本投稿されます。
-   実行結果は、リポジトリの **Actions** タブから確認できます。

### 手動投稿

いますぐに記事を投稿したい場合は、以下の手順で手動実行が可能です。

1.  リポジトリの **Actions** タブに移動します。
2.  左側のワークフローリストから「**Auto Blog Post - Beauty Clinic**」を選択します。
3.  「**Run workflow**」ドロップダウンをクリックし、再度「**Run workflow**」ボタンを押します。

### キーワードのカスタマイズ

-   生成される記事のテーマは `seo_keywords.json` ファイルに基づいています。
-   新しい施術や地域、特定の悩みに合わせたキーワードを追加・編集することで、より戦略的なコンテンツ生成が可能です。
-   `keyword_usage_rules` に基づき、キーワードの使用頻度が調整されます。

## 5. 生成されるデータ

-   **記事データ**: `data/articles/` に、生成された記事のJSONファイルとMarkdownファイルが保存されます。
-   **キーワード履歴**: `data/keyword_history.json` に、過去30日間に使用されたキーワードとその日時が記録されます。
-   **投稿履歴**: `data/post_history/posts.json` に、WordPressに投稿された記事の情報（ID、URL、キーワードなど）が記録されます。

これらのデータは、ワークフロー実行時に自動でリポジトリにコミット・プッシュされます。
